# Phase 1 QA Clearance - Codex Runtime And Artifact Persistence

- stop_status: `CLEAR_CURRENT_PHASE`
- reviewed_phase: `10-phase-1-codex-runtime-and-artifact-persistence.md`
- branch: `main`
- implementation_commit_sha: `00ec1c7ae1375d23ed838cf0aa35ba2d27d7657d`
- worker_stop_report_commit_sha: `28abdb9a2866ee8e5e43670680de3491d1becc2c`
- pushed_checkpoint_head: `a224f5ef518adc6d290f18f903056ae3d2777b2b`
- qa_stop_report_commit_sha: `pending-qa-report-commit`
- updated_at: `2026-04-29T21:24:15Z`
- next_authorized_phase: `20-phase-2-observability-health-and-installer-semantics.md`

## Findings

No blocking implementation findings were found.

## QA Evidence

Verified checkpoint hygiene:

- Current branch is `main`.
- Recorded implementation SHA resolves to `00ec1c7ae1375d23ed838cf0aa35ba2d27d7657d`.
- Recorded worker report SHA resolves to `28abdb9a2866ee8e5e43670680de3491d1becc2c`.
- Pushed checkpoint head is `a224f5ef518adc6d290f18f903056ae3d2777b2b`.
- The only working-tree dirt before QA-owned updates was pre-existing untracked `tools/forge/__pycache__/`, which remains out of scope.

Reviewed implementation surfaces:

- `docker-compose.yml`
- `scripts/install-vps.sh`
- `packages/adapters/src/codex-runner.ts`
- `tools/live-external-smoke.ts`
- `tests/codex-runner.test.ts`
- `tests/vps-installer.test.ts`
- `docs/deployment/README.md`
- `docs/deployment/vps.md`

Independent verification passed:

```bash
npm run test -- --run tests/codex-runner.test.ts tests/vps-installer.test.ts tests/telegram-workflow-api.test.ts
npm run verify
docker compose build
AUTO_FORGE_HOST_DATA_DIR=/tmp/auto-forge-qa-phase1-proof AUTO_FORGE_API_PORT=3302 AUTO_FORGE_WEB_PORT=5575 docker compose up -d postgres api worker web
AUTO_FORGE_HOST_DATA_DIR=/tmp/auto-forge-qa-phase1-proof AUTO_FORGE_API_PORT=3302 AUTO_FORGE_WEB_PORT=5575 docker compose logs --tail=100 api
AUTO_FORGE_HOST_DATA_DIR=/tmp/auto-forge-qa-phase1-proof AUTO_FORGE_API_PORT=3302 AUTO_FORGE_WEB_PORT=5575 docker compose -f docker-compose.yml -f docker-compose.smoke.yml run --rm smoke
AUTO_FORGE_HOST_DATA_DIR=/tmp/auto-forge-qa-phase1-proof AUTO_FORGE_API_PORT=3302 AUTO_FORGE_WEB_PORT=5575 docker compose down --remove-orphans
```

Additional installer-shape container proof passed after creating the host data directories exactly as `prepare_runtime_data_dirs` does:

- API container reported `CODEX_HOME=/data/codex-home`, `AUTO_FORGE_CODEX_AUTH_SOURCE_DIR=/codex-auth-source`, `AUTO_FORGE_ARTIFACT_ROOT=/data/artifacts`, and `AUTO_FORGE_PROMPT_ROOT=/data/prompts`.
- API container confirmed active `CODEX_HOME` was writable and separate from the read-only OAuth auth-source mount.
- Worker container confirmed active `CODEX_HOME` was writable and separate from the OAuth auth-source mount.
- Proof files under `/data/prompts` and `/data/artifacts` were writable with mode `0600`.

## Limitations

- Live Telegram/OpenClaw/OpenAI/GitHub deploy-key proof was not part of Phase 1 and remains reserved for a later gate.
- A raw Compose run against an uninitialized host data directory starts services, but the installer-prepared directory shape is the production deployment contract for writable Codex runtime, prompt, and artifact paths.
- The separate Postgres workflow-store durability gap remains out of scope for this phase.

## Durable Memory Candidates

- Deployed containers use `/data/codex-home` as writable active `CODEX_HOME`; the protected OAuth source cache is mounted read-only at `/codex-auth-source`.
- Compose runner prompts and artifacts persist under `/data/prompts` and `/data/artifacts`.
- `AUTO_FORGE_CODEX_AUTH_SOURCE_DIR` is the installer/Compose host-side OAuth source override; legacy `AUTO_FORGE_CODEX_HOME_DIR` remains accepted as a source-path compatibility alias.
- Codex runner blocker summaries classify read-only runtime homes, permission failures, missing auth, and missing CLI without exposing raw secret values in Telegram-facing text.

## Next Authorized Window

Phase 2 is authorized:

- `20-phase-2-observability-health-and-installer-semantics.md`
