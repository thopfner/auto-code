# Runtime Health Repair - Source Ready For Target Validation

- stop_status: `READY_FOR_TARGET_VALIDATION`
- reviewed_phase: `30-phase-3-vps-telegram-proof.md`
- branch: `main`
- implementation_commit_sha: `be393aaf2177e52ca381f46cbacfc4cc1e9d2bb8`
- source_candidate_commit_sha: `be393aaf2177e52ca381f46cbacfc4cc1e9d2bb8`
- prior_phase_stop_report_commit_sha: `22e45e87aec8221599e03ee4fc58b23da7a49780`
- target_install_path: `/opt/auto-forge-controller`
- updated_at: `2026-04-30T05:26:21Z`
- next_authorized_phase: `30-phase-3-vps-telegram-proof.md`

## Summary

The source repair addresses the false health degradations reported after the latest deployment attempt.

- Host runtime env generated by `setup:vps` now includes database config, explicit `AUTO_FORGE_RUNTIME_CONTEXT=host`, host data mount metadata, and container `/data/*` health paths that map back to the host Compose data directory.
- Compose services now override `AUTO_FORGE_RUNTIME_CONTEXT=container`; API health checks use `http://127.0.0.1:3000/live` for the local API and `http://web:5173/` for web reachability inside Compose.
- Worker heartbeat files are readable by host diagnostics because they are written mode `0644`; the heartbeat contains only pid, runner, and timestamp health data.
- The Docker image installs `ca-certificates`, and ESLint ignores `.auto-forge/**`.

## Validation Completed In Source/Disposable Compose

```bash
npm run test -- --run tests/ops.test.ts tests/vps-setup-wizard.test.ts tests/vps-installer.test.ts
npm run verify
AUTO_FORGE_HOST_DATA_DIR=/tmp/auto-forge-health-proof AUTO_FORGE_API_PORT=3303 AUTO_FORGE_WEB_PORT=5576 docker compose build
AUTO_FORGE_RUNTIME_ENV_FILE=/tmp/auto-forge-health-proof.env AUTO_FORGE_HOST_DATA_DIR=/tmp/auto-forge-health-proof AUTO_FORGE_API_PORT=3303 AUTO_FORGE_WEB_PORT=5576 docker compose up -d postgres api worker web
AUTO_FORGE_RUNTIME_ENV_FILE=/tmp/auto-forge-health-proof.env AUTO_FORGE_HOST_DATA_DIR=/tmp/auto-forge-health-proof AUTO_FORGE_API_PORT=3303 AUTO_FORGE_WEB_PORT=5576 docker compose exec -T api sh -lc 'dpkg -l ca-certificates && test -r /etc/ssl/certs/ca-certificates.crt'
AUTO_FORGE_RUNTIME_ENV_FILE=/tmp/auto-forge-health-proof.env AUTO_FORGE_HOST_DATA_DIR=/tmp/auto-forge-health-proof AUTO_FORGE_API_PORT=3303 AUTO_FORGE_WEB_PORT=5576 docker compose exec -T api npm run ops:health
AUTO_FORGE_RUNTIME_ENV_FILE=/tmp/auto-forge-health-proof.env AUTO_FORGE_HOST_DATA_DIR=/tmp/auto-forge-health-proof AUTO_FORGE_API_PORT=3303 AUTO_FORGE_WEB_PORT=5576 docker compose exec -T worker npm run ops:health
set -a; . /tmp/auto-forge-health-proof.env; set +a; npm run ops:health
AUTO_FORGE_RUNTIME_ENV_FILE=/tmp/auto-forge-health-proof.env AUTO_FORGE_HOST_DATA_DIR=/tmp/auto-forge-health-proof AUTO_FORGE_API_PORT=3303 AUTO_FORGE_WEB_PORT=5576 docker compose down --remove-orphans
```

Observed:

- Focused tests passed: 39 tests.
- Full verification passed: 17 files, 112 tests.
- Docker build passed and installed `ca-certificates`.
- API-container `npm run ops:health` passed with web URL `http://web:5173/`.
- Worker-container `npm run ops:health` passed with web URL `http://web:5173/`.
- Host `npm run ops:health` passed with setup, worker, database, API, web, and Codex checks passing against the disposable runtime env.

## Target Validation Still Required

This source/dev checkout still is not the deployed runtime. `/opt/auto-forge-controller` is absent on this host, so the real target deployment and Telegram smoke were not run here.

After this commit is available on GitHub, deploy on the target with the standard target flow:

```bash
cd /opt/auto-forge-controller
git pull --ff-only
sudo bash scripts/install-vps.sh
```

Then rerun the Phase 3 acceptance sequence on the target:

```bash
set -a
. /etc/auto-forge-controller/auto-forge.env
set +a
npm run ops:health
docker compose exec -T api npm run ops:health
docker compose exec -T worker npm run ops:health
```

The direct `codex exec` container proof and Telegram `/scope @auto-forge-controller ...` smoke remain required before claiming production readiness.

## Remaining Production Blockers Outside This Repair

- API workflow state is still backed by `MemoryWorkflowStore`; a durable Postgres-backed workflow store remains a separate production blocker.
- Worker service still writes heartbeat and idles; durable queue consumption, retries, repo locks, and recovery ownership remain a separate production blocker.

## Durable Memory Candidates

- Runtime env has explicit host/container context: installer-generated host env uses `AUTO_FORGE_RUNTIME_CONTEXT=host`; Compose services override `AUTO_FORGE_RUNTIME_CONTEXT=container`.
- Host diagnostics map `/data/*` health paths back to `AUTO_FORGE_HOST_DATA_DIR`; containers must keep `/data/*` unchanged and use Compose service DNS for peer health checks.
