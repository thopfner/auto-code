# Phase 2 QA Clearance - Observability, Health, And Installer Semantics

- stop_status: `CLEAR_CURRENT_PHASE`
- reviewed_phase: `20-phase-2-observability-health-and-installer-semantics.md`
- branch: `main`
- implementation_commit_sha: `62eeb17c6108c6380cb38b1cf737832556dc9768`
- worker_stop_report_commit_sha: `13fc56d6862fe48181c774553796d1595b274ad1`
- pushed_checkpoint_head: `aec6d831701c7f1b6a67a953f34f3abaa587cc3b`
- qa_stop_report_commit_sha: `5c4063d9b3f2429259dfeab1ea30db6ccf894580`
- updated_at: `2026-04-29T21:41:45Z`
- next_authorized_phase: `30-phase-3-vps-telegram-proof.md`

## Findings

No blocking implementation findings were found.

## QA Evidence

Verified checkpoint hygiene:

- Current branch is `main`.
- Recorded implementation SHA resolves to `62eeb17c6108c6380cb38b1cf737832556dc9768`.
- Recorded worker report SHA resolves to `13fc56d6862fe48181c774553796d1595b274ad1`.
- Pushed checkpoint head is `aec6d831701c7f1b6a67a953f34f3abaa587cc3b`.
- `origin/main` matched local `HEAD` before QA-owned clearance edits.
- The only working-tree dirt before QA-owned updates was pre-existing untracked `tools/forge/__pycache__/`, which remains out of scope.

Reviewed implementation surfaces:

- `packages/adapters/src/codex-runner.ts`
- `packages/core/src/workflow-engine.ts`
- `packages/ops/src/paths.ts`
- `apps/cli/src/index.ts`
- `scripts/install-vps.sh`
- `tests/codex-runner.test.ts`
- `tests/ops.test.ts`
- `tests/vps-setup-wizard.test.ts`
- `tests/vps-installer.test.ts`
- `tests/workflow-engine.test.ts`
- `docs/deployment/README.md`
- `docs/deployment/vps.md`

Independent verification passed:

```bash
npm run test -- --run tests/codex-runner.test.ts tests/ops.test.ts tests/vps-setup-wizard.test.ts tests/vps-installer.test.ts
npm run verify
docker compose ps
```

`docker compose ps` showed no running services in this source/dev checkout. QA did not start another stack because the worker already provided service-scoped disposable Compose evidence and the corrected topology requires avoiding unnecessary long-lived services on `/var/www/html/auto.thapi.cc`.

Accepted coverage:

- Codex deterministic infrastructure failures are classified as `blocked` runner results.
- Generic non-deterministic runner failures keep a bounded redacted Telegram-facing summary while raw process output remains in the persisted log.
- Redaction covers OpenAI keys, bearer tokens, JWTs, Telegram bot tokens, private key blocks, auth JSON fields, and long opaque secret-like values.
- Workflow retry behavior stops retrying deterministic blocked runner failures.
- Host-side ops path resolution maps container `/data/*` values to the host Compose data directory while container services keep `/data/*` unchanged.
- Installer live-smoke behavior remains non-fatal for first-run onboarding but supports `AUTO_FORGE_LIVE_SMOKE_HARD_GATE=1` for production automation.
- VPS setup subprocess tests now have explicit timeouts above the child-process timeout.
- The Phase 2 service validation in the worker report used alternate ports and cleaned down the disposable local Compose stack, aligning with the source/dev topology.

## Limitations

- Live Telegram/OpenClaw/OpenAI proof was not run in Phase 2; Phase 3 is the authorized target-deployment proof window.
- Phase 3 must not use `/var/www/html/auto.thapi.cc` as proof that the product is deployed. It must push/pull the accepted GitHub commit into the target install, commonly `/opt/auto-forge-controller`, before service restart or Telegram/Codex validation.
- The separate Postgres workflow-store durability gap remains out of scope for this brief.

## Durable Memory Candidates

- Codex deterministic infrastructure failures are represented as blocked runner results with redacted operator-actionable summaries, avoiding repeated retries for the same missing CLI/auth/path/write root cause.
- Host CLI health maps container `/data/*` runtime paths to `AUTO_FORGE_HOST_DATA_DIR` or `.auto-forge/compose-data`; containers continue to use `/data/*` directly.
- `AUTO_FORGE_LIVE_SMOKE_HARD_GATE=1` makes installer live-smoke `BLOCKED_EXTERNAL` fail closed for production automation, while first-run onboarding remains non-fatal by default.
- The source/dev checkout topology rule remains active: `/var/www/html/auto.thapi.cc` is not the deployed runtime unless explicitly designated; deployed proof must run from the target install after pulling the accepted GitHub commit.

## Next Authorized Window

Phase 3 is authorized:

- `30-phase-3-vps-telegram-proof.md`
