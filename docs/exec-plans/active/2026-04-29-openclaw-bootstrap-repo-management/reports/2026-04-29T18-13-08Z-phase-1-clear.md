# Phase 1 QA Clearance - Managed OpenClaw Bootstrap And Telegram Ownership

- stop_status: `CLEAR_CURRENT_PHASE`
- phase_reviewed: `11-phase-1-revision-telegram-ownership.md`
- accepted_implementation_commit_sha: `aa034144215d78d0ae855e98d64037e69b4e2920`
- worker_stop_report_commit_sha: `c8bcea2eb69a42ed5a9aac4c4ea8f2473f4132c6`
- worker_metadata_head: `463f163c76be99e6d4eed337dc851f4f09529c6c`
- qa_clearance_commit_sha: `PENDING_QA_CLEARANCE_COMMIT`

## Findings

No blocking findings remain.

The revision removed default same-bot OpenClaw Telegram channel provisioning while preserving the managed OpenClaw workspace bootstrap.

## Verified Behavior

- `OPENCLAW_SETUP_MODE=install-or-onboard` still runs the managed OpenClaw workspace bootstrap.
- OpenClaw local gateway config remains managed and validated.
- The installer disables OpenClaw's Telegram channel by default with `openclaw config set channels.telegram.enabled false`.
- The installer no longer writes `/root/.openclaw/telegram-bot-token`.
- The installer no longer writes `TELEGRAM_BOT_TOKEN` into `/root/.openclaw/.env`.
- The installer no longer sets OpenClaw `channels.telegram.tokenFile` or `channels.telegram.defaultTo`.
- Auto Forge remains the Telegram webhook owner through `${PUBLIC_BASE_URL%/}/telegram/webhook`.
- Live smoke keeps OpenClaw CLI Telegram delivery optional unless `AUTO_FORGE_REQUIRE_OPENCLAW_TELEGRAM_DELIVERY=1` is explicitly set.

## Checks Run By QA

```bash
bash -n scripts/install-vps.sh
bash -n scripts/setup-openclaw.sh
npm run test -- --run tests/vps-installer.test.ts tests/openclaw-bootstrap.test.ts
npm run verify
npm run full-rebuild
```

Results:

- bash syntax checks passed
- targeted tests passed: 2 files, 20 tests
- `npm run verify` passed: 16 files, 91 tests
- `npm run full-rebuild` passed, including fresh bootstrap, verify, install-check, health, backup/restore dry run, recovery/log discovery, Docker Compose build/up/smoke, and cleanup

## Limitations

`npm run live:smoke` was not run in this local QA shell because live Telegram/OpenClaw/OpenAI credentials are not available here. The test VPS should rerun live smoke after pulling this clearance if external validation is needed before continuing.

An unrelated untracked path remains: `tools/forge/__pycache__/`. It existed before this QA pass and was left untouched.

## Durable Memory Candidates

- Default VPS install must not configure OpenClaw Telegram channel ownership for the Auto Forge bot.
- Auto Forge owns inbound Telegram through `${PUBLIC_BASE_URL%/}/telegram/webhook`.
- OpenClaw Phase 1 role is managed local gateway/workspace helper; any future OpenClaw Telegram channel must be explicit and separate from the Auto Forge bot ownership path.

## Next Authorized Window

Phase 2 is now authorized:

- `20-phase-2-safe-telegram-repo-registry.md`

Do not start Phase 3 until Phase 2 reaches and clears its QA checkpoint.

